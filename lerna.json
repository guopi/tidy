{
    "version": "independent",
    "packages": [
        "packages/*"
    ],
    "npmClient": "yarn",
    "command": {
        "publish": {
            "ignoreChanges": [
                "__tests__/**",
                "*.txt"
            ],
            "message": "chore(release): publish"
        }
    }
}
