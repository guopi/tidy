{
    "version": "independent",
    "packages": [
        "packages/*"
    ],
    "npmClient": "yarn",
    "command": {
        "publish": {
            "ignoreChanges": [
                "__tests__/**",
                "*.txt",
                "*.md"
            ],
            "message": "chore(release): publish"
        }
    }
}
